extends: site.yaml

mode: production
media_url: /~kgulliks/media
base_url: /~kgulliks
deploy_root: deploy_production

publisher:
    ssh:
        type: hyde.ext.publishers.ssh.SSH
        username: kgulliks
        server: astro.as.utexas.edu
        target: /home/astro/edu/kgulliks/www
